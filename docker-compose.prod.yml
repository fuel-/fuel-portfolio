services:
  portfolio:
    build: .
    container_name: portfolio-app
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    volumes:
      - portfolio-data:/data
    networks:
      - caddy-net

volumes:
  portfolio-data:

networks:
  caddy-net:
    external: true
